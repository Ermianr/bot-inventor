# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Downloaded by `bun run sidecar`: the pinned Node.js the app ships as a
# sidecar. It is 50MB of somebody else's binary and does not belong in git.
/binaries/

# Built by `bun run sidecar`: the Runtime a Session runs against.
/resources/
