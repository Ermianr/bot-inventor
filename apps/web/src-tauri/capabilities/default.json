{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The default permissions, plus the file dialogs a Project is saved and opened through, and closing the window: listening for the close asks the editor first, and it is then the editor that closes the window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-confirm"
  ]
}
