{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The default permissions, plus the dialogs an Export asks its destination through: where the folder goes, and whether an Export already there may be replaced. A Project needs none of them — the application owns where Projects live.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-confirm"]
}
